foreach (var attachment in article_attachments)
{
    // Process each attachment
    // 'attachment' represents each item in the 'article_attachments' collection
    // Perform actions on 'attachment'
}
